[project]
name = "refero-scraper"
version = "0.1.0"
description = "Scrape styles.refero.design: extended DESIGN.md + preview mp4 per style, one folder each"
requires-python = ">=3.10"
dependencies = [
    "httpx[http2]>=0.27",
    "playwright>=1.44",
    "tqdm>=4.66",
]

[tool.uv]
package = false
